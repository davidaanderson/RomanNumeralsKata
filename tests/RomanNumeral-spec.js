'use strict';

describe('the RomanNumeral class', function () {
    describe('when constructed with a roman numeral', function() {
        it('should be displayed with toString', function () {
            expect(true).to.be.false;
        });   
    });
});
